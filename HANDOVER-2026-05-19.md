# Handover — sturm.0711.io / elster-v5_4 ESE-Pipeline

**Date:** 2026-05-19
**Session focus:** Deterministischer ESE-Extraktor + 8 Bugs aus Click-Through-Review + Person-A/B-Duplikate
**User:** christoph@0711.io
**Repo:** `/Users/christophbertsch/Desktop/sturm.0711.io` (lokal) + Container `sturm:0.1.0` auf h200v
**Tunnel:** `sturm.0711.io` → h200v container port 7800

---

## 1. Was läuft

**Workflow `elster-v5_4`** (registriert, alle Stages grün):

```
systemReadiness → ocr → klassifizierung → {
    lohnsteuerbescheidMapper   (Pfad A: vast_bundle)
    einkommensteuererklaerungMapper  (Pfad B: einkommensteuererklaerung)
    felderKatalog → phase1Regex → phase3LlmFill  (Pfad C: einzelbeleg)
} → finalize
```

**Status auf Stricker-2023-PDF:**
- 7/7 Pre-Flight-Probes grün (Lane 1, Ollama, embeddinggemma, Gemma-4, vLLM, atoms.json 2287 eCodes, FP32-Cascade)
- klassifizierung: doc_type=`einkommensteuererklaerung`, steuerjahr=`2023`, 7 Anlagen erkannt
- **36 echte Locks** (W1=10 Math, W2=20 zone-filtered, W4=6 Cascade @ confidence 0.7)
- **Lane 1 §32a:** ZvE 51.970,02 € · ESt 7.104,46 € · Soli 0 · **Erstattung 220,34 €**
- Ergebnis-Tab UI: aktuell 33 Felder sichtbar (Person-B-Werte für 3 KAP-eCodes verloren — fix gerade deployed, needs re-run)

---

## 2. 5-Wellen-Solver — Architektur

Backend: `tools/elster-inverse-solver/inverse_solver.py` (Python, deterministisch, keine LLMs).

| Welle | Inhalt | Container | Confidence |
|---|---|---|---|
| 1 | Ratio Math: Soli 5,5% · KiSt 8/9% · SV-Ratios (RV 9,3 / KV 7,3 / PV 1,5 / ALV 1,3 %) | atoms.json | 1.0 |
| 1.5 | PSEUDO→echte eCode Transfer | atoms.json | 1.0 |
| 2 | Spatial Zoning per (Anlage, Person) — math-anchors als Zone-Marker | OCR | n/a |
| 3 | Label-Adjacency mit Zone-Filter + Drucktext-Clean (Levenshtein) | atoms.json | 1.0 / 0.6 |
| 4 | embeddinggemma-Cascade: FP32 cosine top-K @ 0.55 Threshold | embeddings.gemma4.fp32.bin (6,7 MB · 2287×768) + Ollama | 0.7 |
| 5 | Lane 1 BMF §32a-Verifier (HTTP POST) | bmf-calculator @ host:12010 | informational |

**WICHTIG — Konventionen:**
- `__A` / `__B`-Suffix für Person-Disambig (Stricker = Einzelveranlagung, aber KAP-Steuern haben oft Person-Splits)
- `PSEUDO_*`-Locks sind interne Math-Anker (KEINE echten eCodes) — werden vom TS-Stage gefiltert
- atoms.json = 0711:elster:bmf:jahresdok-2024:v2 (gitchain-anchored container)
- Statutory constants 2023: Grundfreibetrag 10.908, Arbeitnehmer-Pauschbetrag 1.230, …

---

## 3. Session-Accomplishments (chronologisch)

### 8 Bugs aus UI-Click-Through gefixt

| # | Bug | Fix-Datei | Status |
|---|---|---|---|
| 1 | Cascade silent broken: `OLLAMA_URL` Base-URL ohne `/api/embed` | `welle4_cascade.py` `_normalize_ollama_url()` | ✅ deployed |
| 2 | klassifizierung steuerjahr=`2025` aus Ausfertigungs-Datum statt `2023` aus VZ-Header | `klassifizierung.ts` 3-Prio-Strategy + Datum-Kontext-Filter | ✅ deployed |
| 3 | Ergebnis-Tab leer trotz `fertig` (sucht nur phase5-merge/bmf-rechner, nicht finalize-extraction) | `pipeline.jsx` finalize→phase5Merge mit `provenance`-join | ✅ deployed |
| 4 | Sidebar `+8 weitere`-Truncation versteckt v5_4 | `nav.js`: `.slice(0,6)` raus, active-pin per Category | ✅ deployed |
| 5 | Token-Persistence nur auf `/`, andere Routen → 401 | `auth.ts` 401-page liest `localStorage['sturm-token']` + auto-retry (mit `sessionStorage`-loop-Schutz) | ✅ deployed |
| 6 | Solver-Debug-Files nach `/tmp/ese-solver-last.*` (volatile) | `ese-mapper-stage.ts` via `ctx.artifacts.write()` → `runs/<id>/einkommensteuererklaerungMapper/solver.{stdout,stderr,args,input.ocr}.*` | ✅ deployed |
| 7 | `_result.json.events = []` (Persistence-Lücke zw. Live-SSE und Final-Write) | `runner.ts` Event-Subscriber-Buffer; `types.ts` `RunResult.events?: …` | ✅ deployed |
| 8 | `systemReadiness` im React-Flow disconnected | `index.ts` edge `['systemReadiness', 'ocr']` in v5_4 edges | ✅ deployed |

**Verifizierungs-Run:** `mpc6xvvl-ideus0` (siehe `runs/elster-v5_4/`) — alle 8 Bugs PASS via `/tmp/verify-fixes.py`.

### In-Progress (während letzter Frage unterbrochen)

**Person-A/B-Duplikate retten (36 statt 33 sichtbar):**

3 eCodes haben Person A+B doppelt:
- E1904701 (KESt): A=26,69 € · B=1,99 €
- E1904901 (Soli-KESt): A=1,45 € · B=0,10 €
- E1904801 (KiSt-KESt): A=2,39 € · B=0,17 €

Der TS-Stage kollabierte `__A`/`__B` auf ersten Wert. **Fix deployed** in `einkommensteuererklaerung-mapper-stage.ts`:

```ts
const suffix = key.includes('__') ? key.split('__').pop() : null;
if (!(baseEcode in ecodes)) {
  ecodes[baseEcode] = l.value;
} else if (suffix && !(`${baseEcode}/${suffix}` in ecodes)) {
  ecodes[`${baseEcode}/${suffix}`] = l.value;  // E1904701/B als separater Key
}
```

**Status:** Code deployed in Container. Re-run wurde NICHT mehr verifiziert (User hat Session abgebrochen für Handover).
**Nächster Schritt:** PDF nochmal hochladen, Ergebnis-Tab sollte **36 Felder** zeigen.

---

## 4. Wichtige Klarstellungen (gegen Fehlinterpretation)

**`Locked: 60` aus Solver-stdout ≠ 60 echte eCodes:**

Der manuelle Solver-Run-stdout sagt `After Welle 1: Locked: 60` — das ist **inkl. 30 PSEUDO_*-Math-Hilfsknoten** (PSEUDO_Lohnsteuer, PSEUDO_Soli, …) die als Ratio-Anker dienen. Nach PSEUDO-Filter (im TS-Stage) bleiben **36 echte eCodes** — identisch im Workflow und manuellen Run.

```
Manueller Run (/tmp/solver-test.json):    66 total, 30 PSEUDO  → 36 echt
Workflow eseMapper output.json:           36 (PSEUDO bereits gefiltert)
output.ecodes (flach, base-eCode):        33 (3 Person-A/B doppelt → kollabiert)
finalize.canonical_layer.codes:           33 (folgt eseMapper.ecodes)
```

Mit aktuellem Person-Suffix-Fix sollte das auf 36 steigen.

---

## 5. Server / Container / Deploy

**h200v VM:** `christoph.bertsch@h200v` (SSH-Key konfiguriert, kein Passwort)

**Container:** `sturm:0.1.0` (Image-Tag), Name `sturm`
- läuft mit `tsx` (kein Compile-Step, ESM TypeScript direkt)
- Bind-Mount: `/home/christoph.bertsch/0711/0711-STURM/runs` → `/app/runs`
- Env: `STURM_BEARER_TOKEN`, `OLLAMA_URL`, `LANE1_URL` (von TS-Stage injected mit `host.docker.internal`-Fallback)

**Container-side Python:** `python3` (3.12) — wurde in vorheriger Session via Dockerfile `apk add python3` ergänzt. **NICHT** wieder entfernen.

**Tunnel:** Cloudflared `sturm.0711.io` → `localhost:7800`. Bearer-Token-protected:
- **Token:** `749037d25190a2ff053973d22f1beaa739eca3c33afff1744881d2e5c9858a80`
- Erstaufruf: `https://sturm.0711.io/?token=<TOKEN>` → speichert in localStorage
- Andere Routen funktionieren NACH erstem Aufruf (Fix Bug 5)

### Standard-Deploy-Pattern

```bash
# 1. lokale Edit
# 2. scp → h200v
scp /Users/christophbertsch/Desktop/sturm.0711.io/PATH h200v:/tmp/FILE
# 3. docker cp → container
ssh h200v 'docker cp /tmp/FILE sturm:/app/PATH'
# 4. Falls UI (pipeline.jsx/designer.jsx): rebuild bundle
ssh h200v 'docker exec sturm npm run build:ui:pipeline'
# 5. Falls Server-Code: restart
ssh h200v 'docker restart sturm && sleep 7 && docker logs sturm --tail 5'
```

**Cache-Busting für UI:** Bei pipeline.jsx-Änderungen `src/ui/pipeline.html` Version-Query hochziehen (`?v=20260519b` → `c`).

**Pristine-State recovern** (falls Container-Patches verloren):
```bash
ssh h200v 'docker run --rm sturm:0.1.0 cat /app/PATH > /tmp/pristine.ts'
# dann patchen + zurück deployen
```

⚠️ **Achtung Drift-Risiko:** `src/verticals/elster-v3/index.ts` hat im Container `buildVorjahresKontextWorkflow`-Export der LOKAL fehlt. Bei Deploys dieses Files immer Image-pristine als Basis nehmen + lokale Edits dazu patchen, NIE lokales File 1:1 überschreiben. Siehe `/tmp/patch-index.py` für das letzte Patch-Pattern.

---

## 6. Verzeichnis-Layout

### Lokal (`/Users/christophbertsch/Desktop/sturm.0711.io/`)

```
src/
├── core/
│   ├── runner.ts             # Event-Buffer für _result.json (Bug 7 fix)
│   ├── types.ts              # RunResult.events?: [...] hinzugefügt
│   ├── events.ts, registry.ts, workflow.ts, artifacts.ts
├── server/
│   └── auth.ts               # Token-Persistence Auto-Retry (Bug 5)
├── ui/
│   ├── pipeline.html         # Cache-bust v=20260519b
│   ├── pipeline.jsx          # Ergebnis-Tab finalize-Mapping + flatten (Bug 3)
│   ├── pipeline.bundle.js    # Build: npm run build:ui:pipeline (esbuild)
│   └── nav.js                # Sidebar ohne Truncation, active-pin (Bug 4)
├── workflows/elster/stages/
│   └── klassifizierung.ts    # 3-Prio steuerjahr + Datum-Kontext-Filter (Bug 2)
└── verticals/elster-v3/
    ├── index.ts              # v5_4 Workflow-Def + edges (Bug 8: systemReadiness edge)
    └── stages/
        ├── system-readiness-check.ts        # Pre-Flight (7 Probes)
        ├── einkommensteuererklaerung-mapper-stage.ts  # Python-spawn + ctx.artifacts (Bug 6, +Person-suffix)
        ├── lohnsteuerbescheid-mapper.ts
        └── finalize-extraction.ts           # canonical_layer.{codes,nested,provenance}
```

### Python-Solver (`/Users/christophbertsch/Desktop/DESKTOP/Stricker/workflow/`)

```
inverse_solver.py     # 5-Wellen-Solver, LANE1_URL via os.environ
welle4_cascade.py     # FP32 cosine, _normalize_ollama_url() (Bug 1 fix)
welle4_llm.py         # legacy, nicht aktiv im v5_4 Pfad
```

Im Container: `/app/tools/elster-inverse-solver/{inverse_solver,welle4_cascade}.py`

### Run-Artifacts (h200v)

```
/home/christoph.bertsch/0711/0711-STURM/runs/elster-v5_4/<runId>/
├── _meta.json
├── _result.json           # jetzt MIT events[] (Bug 7)
├── _input/<filename>.pdf
├── systemReadiness/output.json
├── ocr/output.json
├── klassifizierung/output.json
├── einkommensteuererklaerungMapper/
│   ├── output.json
│   ├── solver-input.ocr.txt      # Bug 6: persistierter Solver-Input
│   ├── solver.stdout.log         # Bug 6: alle 5 Wellen dokumentiert
│   ├── solver.stderr.log
│   └── solver.args.json          # Bug 6: {args[], exitCode}
└── finalize/output.json
```

---

## 7. Debug-Werkzeuge

**`/tmp/probe-latest.py` (h200v):** zeigt run state + stages + systemReadiness-probes + eseMapper output
```bash
ssh h200v 'python3 /tmp/probe-latest.py /home/christoph.bertsch/0711/0711-STURM/runs/elster-v5_4/<runId>'
```

**`/tmp/verify-fixes.py` (h200v):** verifiziert alle 8 Bugfixes gegen einen Run
```bash
ssh h200v 'python3 /tmp/verify-fixes.py /home/christoph.bertsch/0711/0711-STURM/runs/elster-v5_4/<runId>'
```

**`/tmp/lock-analysis.py` (h200v):** zerlegt Lock-Counts (Person A/B Duplikate, PSEUDO vs echt, by-wave)

**`/tmp/run-manual.sh` (h200v):** spielt einen Workflow-OCR-Text durch den Solver ohne Pipeline (CLI repro)

**Container-Shell-Debug:**
```bash
# Cascade-Embedding manuell testen
ssh h200v 'docker exec sturm sh -c "OLLAMA_URL=http://host.docker.internal:11434/api/embed python3 -c \"from welle4_cascade import embed_via_ollama; print(len(embed_via_ollama(\\\"Lohnsteuer\\\")))\""'

# Lane 1 HTTP-Test
ssh h200v 'curl -s http://localhost:12010/health'
```

---

## 8. Bekannte offene Punkte

1. **Person-A/B-Fix Verifizierung:** Code deployed, Re-Run noch nicht durchgeführt. Erwartet: **36 Felder** im UI statt 33. PDF: `/Users/christophbertsch/Desktop/DESKTOP/Stricker/Upload/Elster 2023 Stricker - Einkommensteuererklärung.pdf`

2. **Events-Counter im UI zeigt 0 obwohl `_result.json.events` 30 Einträge hat.** Frontend liest Events live via SSE — bei Replay-View (`?run=<id>`) lädt es Events nicht aus `_result.json` nach. **Nicht in der ursprünglichen Bug-Liste, aber spätere Fix-Kandidaten.**

3. **Welle 3 Label-Adjacency hat im Workflow `wave=2` Tag.** Im Code Z.1426/1439 von `inverse_solver.py` werden Conv-Locks + Label-only-Locks mit `wave=2` markiert (sollte semantisch wave=3 sein). Output `by_wave`-Stat zeigt deshalb W3=0. Cosmetic, aber irreführend.

4. **finalize/output.json `nested` ist `{"": "1"}`** — leer/buggy. Direct-Mapper-Pfad in `convertMapToAccepted()` setzt `anlage=""`, deshalb landet alles im leeren Bucket. `nested` ist im UI-Tab "Anlagen-Detail" sichtbar; aktuell unbrauchbar.

5. **SV-Cluster Multi-Anchor false positive 924 EUR:** E2001505 (PV) UND E2004403 (ALV) beide locked auf 924 € — das ist eigentlich eine andere Position (vermutlich Soli oder ähnliches). Bekannt seit V0-Workflow, Cluster-Filter (`MIN_SV_CLUSTER=3`) entschärft, aber nicht eliminiert. Inhaltlich falsch — nicht für Ground-Truth nutzen.

6. **`enableZoningHandoff` per default off** weil Vision-Zoning-line_no-Sync nicht kalibriert ist (block-lokale `zeilen_nr` kollidieren beim Lookup). Solver fällt auf Regex-Parse-Pfad zurück; das funktioniert. Wenn Zoning aktiviert werden soll, muss Block-lokale → Global-line_no Mapping fixed werden.

---

## 9. Memory-Konstanten (siehe `~/.claude/projects/.../MEMORY.md`)

**Regeln, die immer gelten:**
- **No mocks, no hardcoded data** — jeder gezeigte Wert kommt aus echten Quellen (gitchain MCP, bosch-product-catalog MCP, sealed benchmark JSONL); NIEMALS `src/data/*.ts` literal arrays. Spec-Werte zählen auch als Mock wenn als "live" gezeigt.
- **No synthesis of audit/decision layers** — wenn der echte Audit/Enrichment-Tool nicht auf Disk ist, bleibt der Gap explicit. Replikation der Output-Shape (resolution.json, blueprint_source.json) aus Heuristiken ist verboten.
- **research.0711.io scope: Bosch only** — CTAX und climate study sind out of scope auf feat/research-playground bis CB explizit re-opens.
- **research.0711.io decisions:** Single promoter (CB), v5.1 als branch in same repo, anchor als separate button mit cost preview, source of truth = sealed git repos unter `~/gitchain-repos` auf H200V (NICHT gitchain-postgres v1 display).
- **Quantum pilot:** CS5800iAW v5.1 sealed + anchored on Base mainnet (block 45427604, TX 0x6d73ccd8…). Critical security debt: wallet 0x116e… exposed, **muss vor next anchor rotated werden.**
- **Master surfaces aggregate variant assets:** master detail page muss jede Variant-Image/Datasheet/Manual via HAS_VARIANT relations + on-disk variants surface'n; NIEMALS `variants.length=0` als "no variants" interpretieren.

---

## 10. Standard-Wiedereinstieg in neue Session

```bash
# 1. Check Server-Status
curl -sI https://sturm.0711.io/ 2>&1 | head -3
ssh h200v 'docker ps --filter name=sturm --format "{{.Status}}"'

# 2. Letzte Runs anzeigen
ssh h200v 'ls -1t /home/christoph.bertsch/0711/0711-STURM/runs/elster-v5_4/ | head -5'

# 3. Letzten Run prüfen
ssh h200v 'python3 /tmp/probe-latest.py /home/christoph.bertsch/0711/0711-STURM/runs/elster-v5_4/<latestRunId>'

# 4. Falls Container down: pristine state recovern
ssh h200v 'docker logs sturm --tail 20'
# Bei Boot-Fehler ("missing export X"): image-pristine als basis nehmen + patches anwenden
```

**Sturm-URL für Browser-Testen:**
```
https://sturm.0711.io/?token=749037d25190a2ff053973d22f1beaa739eca3c33afff1744881d2e5c9858a80
```
(Token wird beim ersten Aufruf in localStorage gespeichert, alle Sub-Pages dann ohne `?token=` zugänglich.)

**Test-PDF für ESE-Pipeline:**
```
/Users/christophbertsch/Desktop/DESKTOP/Stricker/Upload/Elster 2023 Stricker - Einkommensteuererklärung.pdf
```

---

## 11. Immediate-Next-Steps (für nächste Session)

1. **Re-Run mit Person-Suffix-Fix verifizieren** (Code deployed, Verify pending)
   - PDF in Browser hochladen, Workflow starten
   - Erwartung: Ergebnis-Tab 36 Felder inkl. `E1904701/B`, `E1904901/B`, `E1904801/B`
2. **Falls 36 PASS:** Person-Suffix auch in finalize-extraction.canonical_layer.codes verifizieren (sollte automatisch durch convertMapToAccepted fließen, aber drucktext/anlage bleiben leer — UI rendert das OK)
3. **Cleanup:** `mcp__ccd_session__spawn_task` für die 5 bekannten offenen Punkte (siehe §8) als separate Tasks abzweigen
4. **Optional:** SV-Cluster-Filter härten (MIN_SV_CLUSTER auf 4 erhöhen) damit 924-false-positive verschwindet — Risiko: legitime Person-A SV-Beiträge mit nur 3 Anchors verlieren

---

**Ende Handover.** Bei Fragen: `christoph@0711.io` direkt.
