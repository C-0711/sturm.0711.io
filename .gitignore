node_modules/
*.log
logs/
.env
.env.*
!.env.example

# Run-Artefakte (sensible Case-Daten)
runs/
uploads/
var/
# Generierte E10-XMLs sind IMMER ausgefüllte Steuererklärungen = Case-Daten
*.e10.xml
# Lokale Session-Zusammenfassungen (wie qc_*)
SESSION-SUMMARY.html

# User-defined workflows (designer-authored) — kept local
workflows-user/

# Legacy-MVP hat eigenen Lifecycle
legacy/*/node_modules/
legacy/*/uploads/

# Editor / OS
.DS_Store
.vscode/
.idea/
.claude/settings.local.json
.claude/worktrees/

# Snapshot-Filter (running-state push 2026-05-04)
.master-key.json
*.bak
*.bak.[0-9]*
*.dummy/
*.tmp/
workspaces/
_staging/

# Added 2026-05-10 (consolidation) — runtime secrets, never commit
.anchor-api-key
.master-key.json
.wallet-rotation-*.txt
*.bak.*
*.bak
.venv
venv
__pycache__
*.pyc

# Local credentials dumps (never commit)
*.rtf
.h200v-creds*
0711-ALLES.*

# Claude Code session transcripts / build summaries (local-only context)
qc_ElsterV3_Container.*

# Stricker-pivot backup (local-only)
src/verticals/elster-v3/data/.backup-*

# editor settings — local only
.claude/settings.json

# Persistente Application-Instances (Phase 4a)
/applications-data/

# Application-Workspaces (per-case state — written at runtime)
# Anchored to repo root so src/applications/ stays tracked.
/applications/

# E2E / probe / shootout report dirs — runtime artifacts
/reports/
